[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Local dev proxy so `netlify dev` serves the function at /api/verify
[[redirects]]
  from = "/api/verify"
  to = "/.netlify/functions/verify"
  status = 200
